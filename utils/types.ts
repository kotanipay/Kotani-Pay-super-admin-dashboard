export interface UserDetails {
	createdAt?: string;
	email?: string;
	id?: string;
	name?: string;
	phoneNumber?: string;
	_id?: string;
}

export interface UserData {
	numberOfUsers: number;
	_id: string;
}
